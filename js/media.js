(() => {
  const esc=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[m]));
  function initBeforeAfter(){const root=document.getElementById('before-after-grid');if(!root||!window.MBData)return;const items=window.MBData.getBeforeAfter();root.innerHTML=items.length?items.map(x=>`<article class="before-after-card"><div class="before-after-images"><figure><img src="${esc(x.before||x.before_url)}" alt="Before: ${esc(x.title)}" loading="lazy"><figcaption>Before</figcaption></figure><figure><img src="${esc(x.after||x.after_url)}" alt="After: ${esc(x.title)}" loading="lazy"><figcaption>After</figcaption></figure></div><div class="before-after-copy"><span class="eyebrow">Repair Story</span><h3>${esc(x.title)}</h3><p>${esc(x.description||'')}</p>${(x.video||x.video_url)?`<video class="before-after-video" controls preload="metadata" src="${esc(x.video||x.video_url)}"></video>`:''}</div></article>`).join(''):'<div class="media-empty"><p>Repair transformations will appear here as the workshop publishes them.</p></div>';}
  function initCredentials(){const root=document.getElementById('credentials-grid');if(!root||!window.MBData)return;const items=window.MBData.getCredentials();root.innerHTML=items.length?items.map(x=>`<article class="credential-card"><a href="${esc(x.image||x.image_url)}" target="_blank" rel="noopener"><img src="${esc(x.image||x.image_url)}" alt="${esc(x.title)}" loading="lazy"></a><div class="credential-copy"><span class="eyebrow">Verified credential</span><h3>${esc(x.title)}</h3><p class="credential-meta">${esc(x.issuer||'')}${x.year?` · ${esc(x.year)}`:''}</p><p>${esc(x.description||'')}</p></div></article>`).join(''):'<div class="media-empty"><p>Workshop approvals and certificates will appear here as they are published.</p></div>';}
  window.initBeforeAfter=initBeforeAfter;window.initCredentials=initCredentials;
  window.addEventListener('mb:data-hydrated',()=>{initBeforeAfter();initCredentials();});
})();
