(() => {document.addEventListener('DOMContentLoaded',async()=>{const form=document.getElementById('admin-login-form'),err=document.getElementById('admin-login-error');if(!form)return;try{const u=await window.MBAAuth?.getUser?.();if(u){location.replace('/mbac-control-7x4k9/portal');return;}}catch{}form.addEventListener('submit',async e=>{e.preventDefault();const btn=form.querySelector('button'),username=document.getElementById('admin-username').value.trim(),password=document.getElementById('admin-password').value;if(btn)btn.disabled=true;if(err)err.hidden=true;try{await window.MBAAuth.adminSignIn(username,password);location.replace('/mbac-control-7x4k9/portal');}catch(ex){if(err){err.hidden=false;err.textContent=ex?.message||'Incorrect username or password.';}}finally{if(btn)btn.disabled=false;}});});})();
